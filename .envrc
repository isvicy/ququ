# NixOS CUDA 支持
# 暴露 CUDA 库给 uv 管理的 Python 环境
if [[ -d "/run/opengl-driver/lib" ]]; then
  export LD_LIBRARY_PATH="/run/opengl-driver/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
fi

# 可选：自动激活 uv 虚拟环境
if [[ -d ".venv" ]]; then
  source .venv/bin/activate
fi
